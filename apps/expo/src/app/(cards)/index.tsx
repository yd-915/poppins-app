import { SafeAreaView, Text, View } from "react-native";

const CardsScreen = () => {
  return (
    <SafeAreaView>
      <View className="grid h-full w-full bg-slate-950">
        <Text>Cards settings</Text>
      </View>
    </SafeAreaView>
  );
};

export default CardsScreen;
